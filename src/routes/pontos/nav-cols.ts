// Numeric columns of the navigator, shared by the rows and the column header so
// the "pt / R$ / teto" labels sit exactly over their numbers.
export const NAV_COLS = { pt: 'w-9 shrink-0 text-right', brl: 'w-14 shrink-0 text-right', cap: 'w-7 shrink-0' } as const;
